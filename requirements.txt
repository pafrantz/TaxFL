# Verified with the versions below (CPU). Newer compatible versions should also work.
torch>=2.2
torch_geometric>=2.5
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7   # only for taxfl.py (Cora curve plot)

# Versions used to produce the v11 numbers:
#   torch 2.12.0, torch_geometric 2.8.0, scikit-learn 1.8.0, numpy 2.4.4
